export type TweetTruncatorOptions = {
    defaultPrefix?: string;
    template?: string;
    truncatedOrder?: string[];
    elisionMark?: string;
    twitterTextOptions?: any;
};
export type TweetTruncatorContents = {
    desc: string;
    quote: string;
    title: string;
    url: string;
    tags: string[];
};
export default class TweetTruncator {
    private template;
    private defaultPrefix;
    private readonly truncatedOrder;
    private elisionMark;
    private twitterTextOptions;
    constructor(options?: TweetTruncatorOptions);
    getTweetLength(str: string): number;
    joinContents(contents: TweetTruncatorContents): string;
    extractTemplate(prefix: string, template: string, contents: TweetTruncatorContents): string;
    truncateStatus(contents: TweetTruncatorContents, overLength?: number): string;
    truncateContent(content: string, overLength: number): string;
}
//# sourceMappingURL=tweet-truncator.d.ts.map